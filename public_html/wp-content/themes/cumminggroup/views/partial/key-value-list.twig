<dl class="key-value {{ utilities }} {{ type }}">
	{% if heading %}<h3 class="type-detail hed-accent">{{ heading }}</h3>{% endif %}
	{% for item in items %}
	<div class="sidebar">
		<dt class="type-detail">{{ item.key }}</dt>
		<dd>{{ item.value }}</dd>
	</div>
	{% endfor %}
</dl>