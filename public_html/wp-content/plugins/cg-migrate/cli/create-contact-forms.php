<?php

function cg_cli_delete_existing_forms($dry_run = false) {
  // Backup and remove all existing gravity forms data
}

function cg_cli_create_new_forms($dry_run = false) {
  // Can't be done via PHP yet; must import these manually.
}